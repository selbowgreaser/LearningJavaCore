1. Почему в Java строго определены диапазоны допустимых значений и области действия простых типов?
Ответ:      для сохранения переносимости программ.

2. Что собой представляет символьный тип в Java и чем он отличается от символьного типа в ряде других языков программирования?
Ответ:      В отличие от других языков, в Java символы не являются 8-битовыми значениями.
            Вместо этого в Java используется кодировка Unicode, позволяющая представлять символы всех письменных языков.
            В Java тип char хранит 16-разрядное значение без знака в диапазоне от 0 до 65536.

3. Переменная типа boolean может иметь любое значение, поскольку любое ненулевое значение интерпретируется как истинное. Верно или неверно?
Ответ:      неверно.

4. Допустим, результат выполнения программы выглядит следующим образом.
Один
Два
Три
Напишите строку кода с вызовом метода p r i n t l n (), где этот результат выводится в виде одной строки.
Ответ:      System.out.println("Один\nДва\nТри");

5. Какая ошибка допущена в следующем фрагменте кода?
for(i =0; i < 10; i++) {
int sum;
sum = sum + i;
}
System.out.println("Сумма: " + sum);
Ответ:      не указан тип переменной i, не проинициализирована переменная sum, а также обращение к sum из внешней области.

6. Поясните различие между префиксной и постфиксной формами записи оператора инкремента.
Ответ:      в префиксной мы сначала увеличиваем, затем возвращаем значение, а в постфиксной - наоборот.

7. Покажите, каким образом укороченный логический оператор И может предотвратить деление на нуль.
Ответ:      System.out.println( 1 == 0 && 2 / 0);

8. До какого типа повышаются типы b y te и s h o r t при вычислении выражения?
Ответ:      до типа int.

9. Когда возникает потребность в явном приведении типов?
Ответ:      когда значения автоматически не приводятся к необходимому типу данных.

10. Напишите программу, которая находила бы все простые числа в диапазоне от 2 до 100.
Ответ:      PrimeNumbers.java

11. Влияют ли лишние скобки на эффективность выполнения программ?
Ответ:      не влияют.

12. Определяет ли блок кода область действия переменных?
Ответ:      да, определяет.

